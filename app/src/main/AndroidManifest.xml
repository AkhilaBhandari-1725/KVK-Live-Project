<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vertex.kvksolapur">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        package="info.androidstation.networkonmainthread"
        android:allowBackup="true"
        android:icon="@mipmap/kvk_icon_two"
        android:label="@string/app_name"
        android:minSdkVersion="8"
        android:supportsRtl="true"
        android:targetSdkVersion="21"
        android:theme="@style/AppTheme"
        android:versionCode="1"
        android:versionName="1.0">
        <activity android:name=".ChatActivity"></activity>
        <activity android:name=".SubFragmentsActivity" />
        <activity android:name=".SubCategoryFragmentsActivity" />
        <activity android:name=".SingleInfoActivity" />
        <activity android:name=".StartActivity" />
        <activity
            android:name=".RegistrationActivity"
            android:label="@string/label_one" />
        <activity android:name=".SingleViewActivity" />
        <activity android:name=".SubCategoryActivity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".HomeCategoryActivity" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>